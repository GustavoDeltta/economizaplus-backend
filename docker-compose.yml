services:
  db:
    image: postgres:15
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ecoplus
      POSTGRES_PASSWORD: ecoplusteam
      POSTGRES_DB: economizaplus
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: